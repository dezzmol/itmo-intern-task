import classes from "../pages.module.scss"

const NotFoundPage = () => {
    return (
        <main className={classes.page_content}>
            not found page
        </main>
    );
};

export default NotFoundPage;